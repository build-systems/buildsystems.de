---
import Button from "./Button.astro";

export interface Props {
  buttonText: string;
  buttonLink: string;
  id: string;
}

const { buttonText, buttonLink, id } = Astro.props;
---

<div>
  <div class="read-more">
    <button class="plus-minus" id={"expand-button-" + id}>+</button><span
      ><Button
        text={buttonText}
        link={buttonLink}
        icon="redirect"
        widthFitContent
      /></span
    >
  </div>
  <p id={"more-text-" + id} class="more-text">
    <slot />
  </p>
</div>

<style>
  .read-more {
    display: flex;
  }

  .more-text {
    padding-top: 1rem;
    font-size: 1.5rem;
    line-height: 1.2;
    height: 0;
    overflow: hidden;
    transition: all ease-in-out 0.15s;
  }

  .plus-minus {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 1.7rem;
    height: 1.7rem;
    font-size: 1.6rem;
    background-color: var(--text-main);
    color: var(--background-body);
    border-radius: 50%;
    margin-right: 1rem;
    cursor: pointer;
    border: 0;
  }

  .plus-minus:hover {
    background-color: var(--background-body);
    color: var(--text-main);
  }

  @media (max-width: 1280px) {
  }
  @media (max-width: 1100px) {
  }
  @media (max-width: 920px) {
  }
  @media (max-width: 625px) {
    .more-text {
      font-size: 1.2rem;
    }
  }
  @media (max-width: 420px) {
    .more-text {
      font-size: 1rem;
    }
  }
</style>
